---
engines:
  duplication:
    enabled: true
    config:
      languages:
      - javascript
  eslint:
    enabled: true
    channel: "eslint-9"
    config:
      config: "eslint.config.mjs"
  fixme:
    enabled: true
    checks:
      XXX:
        enabled: false
      TODO:
        enabled: false

ratings:
  paths:
  - "**.js"
exclude_paths:
- test/
